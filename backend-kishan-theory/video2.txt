====== app banavo and deploy ===== 

npm -v , node -v

=== computer ---><--- server 

=== request aave and response jai ae badhu kaam express nu 
=== postman.com par javo to server hai jo listen kare 
=== server ne aes tarah ka response diya 
=== server ne socha ke aes tarah ka response karo (  )

=== listen 
- server start and listen requests on port 

=== multiple types requests 
1. get,post,patch,put,delete  (database entry delete,update)
2. get = whatever write in url ()

=== /home, /login  sana par listen karvanu che 

===== empty node app how to make 
1. npm init (node package manager thi app initializer karvanu)
or npm init -y (without options direct create )

(aa command thi 1 file create thai package.json)

1. package.json
2. index.js (entry point)
3. version 
4. description 
5. test command etc...

=== have index.js (entry point) create thai 

"scripts": {

    "start": "node index.js", 
    "test": "echo \"Error: no test specified\" && exit 1"

}

== file run karvi hoi to (index.js) , scripts no command 
1. npm run start 
2. node index.js/server.js/main.js

== build, dev like command start 


/// backend mate express joye (web framework)

$ npm install express (documentation jovu)

import express from "express"
const app = express()  (this is object)

(express import and aa express no use kari ne app banavi (variable))

=== 2 type of syntax (styles of js)
1. common js (old)
2. module js (new)

// port 2 types 1. physical (like typec,usb) 2. tej rete virtual port (65000 = length) (aa port thi request listen thai)

// server listen karse koi pan port par (65000 mathi amuk port free hoi )

// app powerful hoi tenathi multiple requests hoi like app.get, app.post 
// home route,login route pe listen karo
// req/res aave to callback aapo req aave to res send karo

// app.get("/" , (req,res) => {

    res.send("hello world")

} )

// aa rete beja route handle thai 
// handle get,post requests 
// based on route send data, send json , file , page pan mokli sako 


/// documentation ma api reference ma requuest , response 
// app.listen request on particular port 

// types of response (res.send(), res.json())
// res.cookie() 
// res.status()


// aama hot reloading n thai like react jevu ()
// computer data ja chuka hai usko vapas lana hai 
// so je new data che te update n thai (so server restart karva pade)
// npm run start
// so code changes kare baar baar to restart karva pade


/// have aa data github par availble karavu che to but some data are sensitive so .gitignore karo
/// port .env ma lakho 

// dotenv install then  .env file banavo aema capital ma variable lakho 
// import dotenv from "dotenv" ;
// dotenv.config() == config karva mate 

/// const PORT = process.env.PORT || 3000 

import express from "express"
import dotenv from "dotenv"

dotenv.config() ;

const app = express() ;
const PORT = process.env.PORT || 3000/4000 (je rakhvu hoi ae)

app.get("/" , (req,res) => {

    res.send("hello kishan")

} )

app.get("/home" , (req,res) => {

    res.send("hello kishan")

} )

/// free resources computing power aape ne heroku , digital ocean, railway, cnode, render



//// render ma free ma deploy karo backend ( build command kai nai lakhvo ) only
run command karvo , root directory proper aapvi ( example here app ) 

/// u can check any github data using link 
https://api.github.com/kishan-312

https://api.github.com/hitesh
















